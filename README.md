# rc_push

## Requirements

## Installation

### Linux

  `sudo install.sh`

### Windows (from PowerShell)

  `& $(where.exe python).split()[0] setup.py install`

## Usage

Check the help running:
  `rc_push.py --help`
